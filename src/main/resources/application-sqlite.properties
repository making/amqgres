amqgres.storage.type=sqlite

# WAL plus a busy_timeout lets the pooled connections share SQLite's single writer without
# SQLITE_BUSY errors. A ':memory:' URL is not usable because each pooled connection would
# open a separate database.
spring.datasource.url=jdbc:sqlite:amqgres.db?journal_mode=WAL&busy_timeout=5000
